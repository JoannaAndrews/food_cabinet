
# implementation plan
user -> add field: isVolunteer (bool)
is false by default
can adjust this in profile by entering code 'CODE'
if code is entered correctly, isVolunteer field for that user is true
until a user selects 'not a volunteer'

# stretch idea
'continue as guest' option for ppl not interested in any subscriptions
just goes to dashboard
allow users to delete account (no longer affiliated w/ Food Not Bombs, etc)